# 项目规则

- 使用中文交流，使用完整、自然的语句；保留代码中的英文标识符。
- 技术栈：Next.js、Tailwind CSS、Cloudflare Workers、D1、R2。先完成 Wrangler 本地成品，再进行线上发布。
- 前端最低支持 iOS 15.5 Safari。根目录 package.json 的 browserslist 必须保留 `safari >= 15.4`（Next.js 16 默认按 safari 16.4 编译，会输出 iOS 15.5 无法解析的 class static block），`npm run build` 通过 scripts/check-browser-support.mjs 校验产物语法；不得为了提高压缩率删除该配置或跳过这项检查。
- DESIGN.md 是设计规范的唯一来源。优先手机布局、单页切换和 iOS 风格底部导航。
- 动态语言代码文件尽量不超过 400 行；每层目录文件尽量不超过 8 个。本地私人原件不进入通用版本。
- 避免重复逻辑、循环依赖、过度设计和含义不清的代码。
- 复杂工作使用 docs/plans 中的 progress.md 持续追踪，完成全部已授权安排后结束。
- 优先 CLI、API 和程序化浏览器测试，不使用 computer use / control-chrome。
- 行程必须保留来源和当地时区；没有确认的时间、费用、付款人必须明确标注，不能凭空确定。
- 护照、身份证、原始凭证和密码验证数据不得进入公开静态资源或前端 bundle。文件通过鉴权 Worker 从私有 R2 读取。
- 金额使用整数分存储；不同币种分别统计；不得把未知付款人计入任意成员。
- 对可持续适用的新规则更新此文档。进入 Git 仓库后，代码改动必须附带说明进行 commit。
- 改动完成后只 commit 到本地，不要主动 push 或触发部署；push / 上线必须由用户明确提出后才执行。

- 登录页面、输入提示、错误消息和公开前端资源不得透露密码前缀、组成规则、姓名缩写规则或示例密码。
- 产品文案使用直接、客观的功能描述，不使用文艺口号、宣传性英文副标题或装饰性欢迎语。
- 首页主卡片使用机票或火车票式的票券布局，具有两侧缺口和虚线分隔；有真实路线时，左侧展示起点，右侧展示终点。
- 首页在票券状态右侧紧凑展示设备位置与真实当前时间，支持行程设置的常用时区与目的地时区切换；安排仍按真实时间判断，预览只影响安排，不影响当前时钟。
- 底部导航只显示图标和头像，不显示文字标签；保留 aria-label 与选中状态。
- 全站不显示顶部品牌横幅或标题栏；首页当前时间与位置区域按最新要求保留。行程预览状态标注在倒计时区域，返回实时操作放在安排下方。
- 视觉风格按照 CapWords 的纸张背景、淡紫色、圆润排版与实物贴纸设计，具体规范持续维护在 DESIGN.md；不能借改版重新加入已经删除的顶部内容。
- 成员头像使用 用户上传的头像，通过登录鉴权后的私有 R2 接口读取，不将原始头像复制到公开静态目录。

## 导航

- [开源发布进度](docs/plans/open-source/progress.md)
- [部署与维护](docs/deployment.md)
- [旅行助手 Skill 进度](docs/plans/skill/progress.md)
- [路线图与行程海报](docs/plans/route-map/AGENTS.md)

- 位置必须来自浏览器 Geolocation API；只在取得设备坐标后查询地名，禁止 IP / 网络估计定位及服务商的 IP fallback。定位失败显示未定位，保留行程时区手动切换。

- 准备 checklist 仅在“完整行程 → 出发前”查看和勾选；首页始终按照时间显示行程安排，不受准备进度影响，个人页不重复展示清单。完整行程在首个日期前保留“出发前”入口，旅途中也可回看。
- 首页不重复展示相关资料列表；安排资料统一通过详情查看。
- 支出必须区分实际付款人与记录人，付款人可从当前行程成员中选择；当前行程的已登录成员均可修改和删除所有支出，保留原始记录人，并校验版本防止覆盖并发修改。新增支出不要求选择分类。
- 支出凭证存入私有 R2，关联已保存支出后供同行成员查看；未关联的上传仅本人可访问。支出直接由用户录入，不提供待核对预订或历史账目确认流程。
- 账本编辑弹窗突出金额，默认展示分摊成员，不再提供备注输入；成员统计与切换控件之间保留明确间距。

- 首页票券下方展示有实际用途的安排提醒、接下来住宿和当天安排；上一项 / 下一项按行程顺序打开详情，不存在的相邻安排不显示按钮。
- 文件查看默认使用大屏模式，不提供“大屏出示 / 返回预览”切换。
- 成员选择统一使用带头像与姓名的自定义组件，禁止使用原生成员 select；分摊成员保持直接可见。
- 凭证选择后立即上传，展示真实上传进度、失败重试与方形图片缩略图，并可点击查看；退出未保存支出时清理上传草稿。
- 个人页可上传、查看和删除仅本人可访问的证件图片；不得伪造缺失的证件照片。

- 记账金额输入聚焦时不显示矩形 outline；记账币种默认使用当前行程的目的地币种，点击按钮仅在目的地币种与常用币种之间切换；显示中文名称，两者相同时禁用切换，不展示全量币种选择器。编辑已有支出时保留原币种，直到用户主动切换。

- 添加支出凭证的入口使用与文件缩略图同尺寸的方形加号按钮，放在同一网格内，不使用文字上传按钮；达到数量上限时隐藏加号。

- 支出详情使用金额卡片、付款人头像、逐人分摊金额及凭证缩略图；底部直接提供修改与删除，删除前确认并刷新账本统计。

- 个人页“我的证件与文件”统一使用整行列表，包括添加入口、上传中和已上传文件；图片左侧展示真实缩略图，其他文件使用文件图标，添加入口使用加号图标。

- 全站 DocumentRow 自动按文件类型显示缩略图，图片显示真实内容，其他文件保留类型图标，不由页面单独开启。本人专属文件必须在服务端过滤列表并校验文件读取权限，“全部资料”也不得包含他人专属文件。

- 文件行的真实图片缩略图继承文件图标的倾斜角度、白边和圆角，不能显示为不倾斜的图片。

- 所有原始资料、头像、上传证件和凭证必须保存在私有 R2；禁止开启 r2.dev 或 R2 Custom Domain。文件只能由 Worker 校验 HMAC 签名会话、会话有效期及成员权限后读取；签名密钥存入 Worker Secret，不进入源码、前端或日志。

- 通用版本默认空白，禁止内置真实成员、行程、凭证、费用、密码规则或旧生产资源信息；注册用户自行创建行程并录入资料。
- 每项旅行业务数据必须关联 trip_id，并在服务端校验当前用户的行程成员资格；个人资料和证件仅本人访问。
- 新版本使用独立 D1/R2，不通过初始化或部署脚本修改旧生产资源。公开源码必须不含私人数据和包含这些数据的旧 Git 历史。

- 登录仅使用邮箱与密码，不要求邮箱验证码；生产注册、忘记密码和旧账号绑定邮箱通过 Cloudflare Email Sending 验证。本地开发跳过邮箱验证，生产环境必须拒绝本地跳过配置。
- 旅行助手与脚本通过设备授权登录：CLI 申请短码并打开 `/device`，用户在浏览器登录并确认后助手才拿到令牌，不要求用户手工生成或复制令牌。令牌只保存 `sha256`，默认 180 天并在剩余不足 150 天时自动续期，带名称显示在“我的 → 已授权的设备”并可撤销。
- 令牌仅通过 `Authorization: Bearer` 使用；只有同时不带 Cookie 的请求，以及不使用 Cookie 的 `/api/device/code` 与 `/api/device/token`，才跳过同源校验。浏览器请求始终校验来源，跨站 Cookie 请求仍然返回 403。
- 设备授权短码与 `device_code` 不可互相推导，兑换一次即失效；未登录访问授权页只能看到登录页，不显示任何账号或行程信息。
- 登录页注册入口使用完整按钮；认证错误提示位于提交按钮上方，不放在页面底部。
- 表单间距按照内容关系分层：标签与输入框紧密关联，字段之间保留分组距离，错误提示靠近提交按钮；不能将所有元素设置为相同间距。具体数值维护在 DESIGN.md。
- 无行程用户注册后进入首页，保留完整底部导航和个人资料、证件管理；首页空状态引导进入行程管理。“我的”提供行程管理入口，管理页必须可以返回，不展示个人信息和退出登录操作。
- 当前 active 行程由应用统一管理，首页、安排、资料和账本使用同一个选择；支持创建和加入多个行程，按账号保存并校验上次选择。
- 当前行程切换仅放在“我的”页面，不显示在导航栏上方或其他内容页面。行程设置、同行邀请、撤销邀请与删除操作统一放在“我的行程”的具体行程管理页面；管理非当前行程不能隐式切换当前行程，接口必须使用被管理行程的 ID。
- 完整行程、旅行资料等已有页面保留原有标题、筛选和内容布局；新增安排、上传资料仅使用标题右侧带 aria-label 的加号按钮，不新增独立操作行或顶部行程信息，避免内容向下移动。
- 注册仅要求邮箱和密码，以及生产环境邮箱验证码；不要求姓名或昵称，昵称在注册后的个人资料中修改。
- 行程邀请使用 6 位大写字母口令，排除 I/O，以数据库约束保证有效口令唯一，每个行程最多一个有效口令；再次进入管理页显示已有口令，主动重新生成才更换。

- 列表没有数据时不得渲染仅有背景、边框或阴影的空容器；空态区分首次使用和筛选无结果，并提供对应说明或操作入口。

- 录入流程面向非技术用户：优先展示最少必要字段，按照安排类型展示相关信息，其余内容按需展开；添加安排分为“类型和名称 → 时间 → 地点 → 资料”四步，支持上一步、下一步并保留所有输入，不显示顶部步骤编号或填写提示。
- 时区与币种必须使用可读中文名称和选择控件，目的地提供默认设置；代码与 UTC 偏移不得要求普通用户填写。跨时区安排保留出发与到达当地时间。
- 日期已知但时间未知的安排必须明确显示时间待定，不生成虚假倒计时；住宿以入住、退房日期为核心。保存安排后返回所属日期。
- 所有上传入口提供可理解的进度和失败重试，安排允许直接上传并关联资料；可见范围必须在上传前明确，个人证件默认仅自己可见。
- 表单保存失败保留输入，关闭时保护未保存内容；头像即时保存不得关闭或清空其他个人资料编辑。

- 安排关联资料使用可选中的图片缩略图方框，非图片显示文件图标。资料上传优先展示分类与文件；分类支持直接输入新名称，可见范围以紧凑的高级设置展示默认值。

- 安排时间使用组件库 TimePicker、Switch 和 Select 组合成统一组件，支持分钟、单个时间、时间段和每端当地时间；选择浮层不得撑高弹窗，保留时间待定。
- 安排的时分与当地时间使用纯选择模式：TimePicker 禁止文本输入，时区 Select 不开启搜索输入，点击不得唤起手机软键盘。
- 地点通过搜索结果选择并保存地址和坐标，不要求用户手动填写详细地址；搜索通过服务端代理，密钥不得进入前端或公开源码。
- 搜索进行时提供加载状态并禁用重复提交；请求失败与没有匹配结果分别提示，不把服务错误显示成空结果。
- 行程中的活动、航班、住宿、自驾与交通统一称“安排”，准备清单的条目统一称“准备事项”；界面文案与无障碍标签不得把两者混用为“事项”。
- 安排表单不展示“安排已确认”复选框；资料上传入口使用关联资料网格中的加号方框，上传成功同时保存到旅行资料并自动选中。

- 所有弹窗统一使用 Sheet、SheetForm 和 SheetFooter，按照内容自然决定高度，仅限制最大高度；标题与底部操作区固定，中间内容独立滚动，类型选择和确认提示不得使用固定高度；关联资料始终展开，缩略图复用旅行资料页的倾斜、白边、圆角与阴影。表单标题与内容区避免叠加大间距，准备清单空态与添加按钮保留明确间距。

- 旅行资料分类使用支持搜索与创建的组件库 Select，已有自定义分类从当前行程可见资料读取，重新打开及刷新后仍可选择。上传文件使用贴纸方框网格与加号入口，真实上传进度在对应方框中央使用圆形进度展示。

- 行程资料预览提供修改名称入口；新名称保存在资料记录中，列表、搜索和安排关联资料统一使用新名称。修改必须校验行程成员与私有权限，保留文件内容和关联关系，下载自动保留正确文件扩展名。

- 产品界面、无障碍标签与错误提示统一使用“资料”，不与“凭证”混用；用户自行填写的名称不自动替换。安排详情的“关联资料”直接进入修改安排的资料步骤，其他修改入口仍从第一步开始，并保留各步骤的数据。

- 安排详情已有相关资料时，列表下方始终提供“添加更多资料”，直接进入资料编辑步骤并保留已选资料。

- [OpenPanel 产品统计计划](docs/plans/analytics/AGENTS.md)：进度与实例配置状态以该目录为准。
- 账号、邮箱验证、行程和多人协作总量以数据库为准，不能用 OpenPanel visitors 或前端按钮点击代替；历史存量不伪装为今日新增。业务事件与业务写入同事务记录，第三方发送失败不能阻止用户使用。分析数据不发送个人信息、自由文本、金额或资料内容；用户与实体使用独立密钥匿名化。看板默认私有，本地不向生产分析实例发送。

- 业务节点的全量统计由后台读取并附加到成功事件；统计查询和第三方上报不得进入注册、邮箱验证等请求的等待流程。快照包含采样时间，持久保存后重试不得重新计算；看板不得将全量快照求和。

- Worker 对外发送分析凭据时使用 redirect: manual 并拒绝非 2xx 响应；禁止使用该运行时不支持的 redirect: error。请求参数必须通过真实 workerd / Miniflare 运行时测试，不能仅依赖 Node fetch mock。

- [行程分享与市场计划](docs/plans/market/AGENTS.md)。公开分享使用创建者确认过的独立快照，采用字段白名单；禁止发布或复制资料关联、账本、成员、证件、电话、预订编号和私人备注。复制得到独立行程，不加入源行程；私人修改不得自动更新公开版本。

- 路线图入口放在完整行程页标题行（添加安排左侧的地图图标），以全屏 Sheet 打开，不新增底部导航项。路线按开始时间排序：航班为虚线大圆弧，其余为实线；每段只在靠近终点的位置放一个方向箭头，并按交通方式加贴纸（航段飞机、其余车辆，放在段中点）；缺坐标安排必须在页面明示并可点开详情，不得静默丢弃或伪造坐标；底图失败降级为站点文本列表。每段贴纸大小随地图缩放与线路长短变化（同缩放下长段更大）；右下角提供逐段播放动画供用户录屏：开场清空路线与编号站点，载具（无圆形底衬）从起点拖线逐段画出，并用稍大的圆点 + 地名标出当前段的起终点，镜头保持当前载具居中并按该段起终点退半级取景，段内 ease-in-out、长段更快，播放中锁定交互与日期筛选。底图地名文字放大一档，编号站点与地名气泡加大；按产品要求隐藏路线图的底图署名（OpenStreetMap/OpenFreeMap 授权要求署名，正式发布前需评估合规或更换底图源）。底图来源常量集中在 `shared/map-source.ts`，前端与 Worker CSP 共用；maplibre-gl 锁 v5，升级前必须通过 `npm run check:browser`。
- 行程海报仅在客户端生成（DOM 模板 + html-to-image），不向服务端上传行程数据；版式为 9:16（1080×1920）全屏地图 + 无圆角底部腰封，海报弹窗不显示标题栏与内边距、地图铺满、关闭按钮悬浮左上，地图取景需预留腰封高度；腰封最后一行展示产品品牌与当前站点域名，按产品要求不展示地图数据来源（授权合规见上条路线图说明）。截图完成前在与海报完全相同的容器上覆盖加载占位（不是地点文字），切换前后尺寸一致。内容不含成员、账本、资料、电话、预订编号、私人备注；地图截图取独立隐藏 MapLibre 实例，生成或关闭后销毁实例。海报入口在路线图标题右侧与「我的行程 → 行程管理 → 分享」区域各一处。

- 单个安排支持手动标记已结束、已取消与恢复。首页的推荐、当日安排、相邻安排和住宿提醒统一排除这些安排；完整行程保留记录并显示状态标签。状态修改不得删除资料、修改计划时间或影响实际预订，普通内容编辑不得重置状态；公开攻略及复制副本不继承个人执行状态。

- 公开行程与分享预览复用首页的左右路线票券及 TravelSticker 贴纸，不以图标文字列表替代。发布不要求勾选，点击发布后在通用弹窗中二次确认公开范围，确认前不得发送发布请求。创建、加入、市场入口统一字号与间距，设置与分享入口保留分组间距。

- 旅行攻略市场公开作者昵称与圆形头像，仅通过仍有效的公开行程 ID 读取作者头像；这是私人头像读取规则的明确例外，R2 仍保持私有，撤销分享后该接口立即拒绝读取。不能公开账号 ID、邮箱、同行人或其他资料。
- 公开分享生成独立的 8 位唯一口令，更新公开内容保留口令，撤销后失效；口令用于市场搜索，不用于加入同行。行程介绍最多 500 字，前后端共同校验，市场灰色区域单行省略、预览展示全文。
- 公开分享可设置覆盖名称，默认使用原行程名；覆盖名统一用于市场卡片、分享预览、搜索和他人复制得到的行程名，不修改原行程，也不因名称变化触发发布 hash 冲突。
- 无行程首页的空状态票券提供“创建行程”和“逛逛旅行攻略市场”两个入口，创建直接进入创建流程，市场直接打开旅行攻略市场；其他页面空状态仍只引导进入行程管理。
- 同行成员区块只在成员人数大于 2 时展示，避免单人/双人行程出现冗余的成员列表。
- 退出登录、删除行程/安排/支出/证件/准备事项等危险操作统一使用红色 danger 样式，并先二次确认再执行。
- 登录页邮箱与密码输入框使用一致的胶囊样式；登录模式底部“注册新账号”与“先看看旅行攻略市场”并排，隐藏“旧账号绑定邮箱”入口（后端能力保留）。
- 并排按钮的数量可能变化时用 flex 加 `flex: 1 1 0; min-width: 0` 自适应平分，单个按钮占满整行；禁止用固定列数的 grid 写死等分，也不能靠事后补样式掩盖半行空白。
- 旅行攻略市场详情复用完整行程的日期横向滚动选择与选中日安排纵向滚动；顶部用作者头像与说话气泡展示行程介绍。
- 全站空态统一使用 EmptyState 组件，图标使用彩色旅行贴纸；空态区分首次使用与筛选无结果。
- 注册时随机分配一个默认贴纸头像；历史用户没有头像时显示昵称第一个字，不得回退为末字。
- 表单保存与删除等操作成功后统一使用全局 toast 提示（`web/src/components/toast.tsx` 的 `useToast`），不在表单内部重复成功文案；失败仍就近显示错误并保留输入。
- 旅行助手 skill 源码放在 `skills/trip-together/`，由 `scripts/build-skill.mjs` 打成 zip 随站点静态资源发布；App 内的「接入 AI」弹窗只指向本站压缩包地址，不使用外部仓库链接。skill 不得包含用户数据，也不得在输出或日志里回显令牌。
