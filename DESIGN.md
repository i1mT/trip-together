# 设计规范 · CapWords 参考风格

## 参考与方向

纸张、实物贴纸、圆润字体、淡紫色和轻盈控件的组合。

## 界面原则

- Mobile first，内容区域最大 560px；320px 宽度也必须可用。
- 单页切换，底部五项悬浮导航，只显示图标和头像，不显示文字；保留 aria-label，白色圆角底座，当前图标使用淡紫色背景，触控区域至少 44px。
- 当前时钟与设备位置放在票券状态（下一项安排 / 正在进行）右侧，使用两行小号文字，点击时间切换时区；不能独立占用首页顶部空间。不加入旅行封面或品牌横幅。
- 首页主卡片采用票券布局：上半部采用左起点、右终点的路线布局，两端显示当地日期和计划时间；没有路线的住宿或活动按实际地点展示。下半部展示倒计时与当前成员；中间使用两侧半圆缺口和虚线表现票根分隔。保留淡紫色票头、纸张纹理和小幅实物贴纸，整张票券可以打开安排详情。
- 安排的日期、计划时间与实时倒计时清楚分层；倒计时按秒更新，时间判断始终保留地点时区。
- 文案直接说明功能、状态、数据和来源，不使用文艺标题或装饰性英文。

## 颜色

| 用途           | 色值              |
| -------------- | ----------------- |
| 纸张背景       | #F6F5F3           |
| 卡片           | #FFFFFF / #FDFCFA |
| 正文与标题     | #292531           |
| 次要说明       | #77727F           |
| 紫色浅底       | #EBE4F4           |
| 主按钮         | #DED1EF           |
| 选中控件       | #E6DCF6           |
| 链接与重点文字 | #6C4C96           |
| 分隔线         | #E8E4EB           |

颜色以 globals.css 与 base.css 为实现来源。避免整块深色账本卡片、厚重边框或渐变背景。以实物贴纸与成员头像提供自然颜色。

## 排版与质感

- 标题使用系统 rounded 字体优先与中文系统字体，字重 750–800，字号 27–33px。
- 日期、计划时间、倒计时和主金额使用 Iowan Old Style / Palatino / Georgia serif，数字使用 tabular-nums。
- 卡片圆角 30–32px，按钮胶囊形，轻柔阴影。表单与列表保留清楚的层级。
- 背景使用低对比细点纹理，以 CSS 实现，不引入庞大纹理文件。
- 实物贴纸来自 imagegen 生成的旅行图集；白色轮廓、柔和阴影，按飞机、汽车、住宿、徒步、证件、行李分配。
- 贴纸辅助识别安排，不挤占安排标题和时间的位置。
- 头像使用用户的真实原图，CSS object-fit 裁切，白边与轻微旋转形成照片贴纸质感。

## 头像

- 注册时随机分配一个默认贴纸头像（复用旅行贴纸图集，圆形裁剪显示）；用户可自行上传头像覆盖。上传头像存储于私有 R2 avatars/ 前缀，登录后经 /api/avatars/:id 读取。
- 应用位置：个人页面、同行成员列表、底部个人导航、支出付款人、成员统计、分摊成员选择。
- 没有上传头像也没有默认头像的历史用户，显示昵称第一个字作为 fallback；加载失败时同样显示昵称第一个字，不得用生成头像替换用户文件。

## 交互与验证

- 可点击区域至少 40px，主要按钮至少 50px。手机输入框使用 16px，避免 iOS 自动缩放。
- 切页轻微淡入，抽屉由底部进入，按下时轻微缩放；尊重 reduced-motion。
- 保留密码遮挡、证件遮挡、登录过期、文件出示下载、记账编辑与状态反馈。
- 保存、删除等操作成功后使用顶部居中的深色胶囊 toast（圆角 14px、13px 字号、约 2.8 秒自动消失），错误使用红色底；toast 覆盖在弹窗之上，不做整屏遮挡。
- 验证 Chromium 与 WebKit，检查所有页面在 320/375/430px 的显示、头像鉴权和控制台异常。

## 并排按钮与自适应布局

- 同一行按钮的数量可能变化时（例如登录页的次要操作行在登录模式有两个按钮、注册模式只剩一个「返回登录」），用 `display: flex` 容器加子项 `flex: 1 1 0; min-width: 0`，让子项平分剩余宽度；只剩一个子项时它自动占满整行。
- 禁止用固定列数的 `grid-template-columns: 1fr 1fr` 表达「按钮可能只有一个」的行，否则单个按钮只占半行，右侧留出空白。固定列数只用于子项数量确定且不会变化的场景。
- 并排按钮不渲染占位空元素；间距由容器的 `gap` 提供（当前为 10px），按钮自身不写左右外边距。
- 文案较长的按钮在窄屏按 `min-width: 0` 收缩并换行，不允许撑破容器或产生横向滚动。

## 贴纸素材

- 文件：web/public/art/travel-stickers.png。
- 使用内置 image_gen 生成，无第三方品牌、文字与成员个人信息。
- 提示词核心：photoreal photographic die-cut travel sticker atlas, six objects, white contour, soft shadow, transparent background; airplane / rental SUV / A-frame cabin / hiking boots / blank passport / butter yellow suitcase。
- 六张贴纸共享一张图集，以 CSS 显示相应区域，不产生重复图片请求。

## 当前时间与位置

- 当前时钟按秒刷新，使用真实时间，不受行程预览影响。
- 使用行程设置中的常用时区和目的地时区，通过一个紧凑按钮互相切换，保存手动选择；夏令时由 IANA 时区处理。
- 位置只通过浏览器 Geolocation API 获取。取得设备坐标后调用反向地理编码查询地名；无坐标时不发送查询，拒绝所有 IP 定位结果。
- 定位失败只显示“未定位”，不通过网络或设备时区猜测城市；用户仍可点击切换时间或重试定位。
- 位置和时间按钮与安排详情按钮相互独立，不能嵌套 button。
- 切换时钟不改变安排判断、票面当地时间或倒计时。

## 出发前准备与账本编辑

- 准备清单仅在“完整行程 → 出发前”展示；分类可展开，逐项使用 checkbox，展示本人完成数量。首页始终显示行程票券，不受准备进度影响。
- 日期导航首项为“出发前”，随后排列旅行日期范围及额外录入的日期；首页不再单独列出相关资料。
- 账本弹窗以金额为视觉重点，输入控件约 42px；付款人与支付日期并排，凭证入口使用轻量行，分摊成员默认展示，不再提供备注输入。

## 文件与成员交互

- 安排票券下排列相邻安排按钮，单侧不存在时使用单个按钮；补充本项提醒、下一住宿卡片和当日安排，不重新加入资料列表。
- 成员选择使用独立选择面板，触发器及选项均展示头像和姓名；列表支持键盘方向键和 Escape 返回。
- 文件查看占用接近完整屏幕，图片等比例展示，保留单独打开和下载操作。
- 记账凭证采用方形缩略图网格，显示真实上传进度、完成或重试状态。金额保持突出，分摊成员默认展开，保存按钮在滚动时保持可见。
- 个人页证件区域标注仅本人可见，不重复展示准备清单。

- 记账金额输入不显示聚焦外框，仅使用文字标签和光标颜色提示；币种使用中文名称按钮，点击仅切换当前行程的目的地币种与常用币种；相同时只显示一种并禁用切换。

- 添加凭证使用缩略图网格中的方形加号入口，尺寸与图片一致，边框使用轻量虚线；按钮内部只显示加号图标。

## 已记录支出详情

- 淡紫色金额卡片展示支出名称、金额、币种和支付日期，付款人头像与姓名放在卡片底部。
- 分摊成员采用三列头像卡片，逐人展示准确到分的分摊金额；凭证使用可点击缩略图。
- 底部固定修改与删除操作，当前行程成员均可操作；删除确认保留支出上下文。原始说明默认折叠。

- 个人证件区使用与机票一致的文件行布局：左侧缩略图或图标，中间名称与辅助信息，右侧操作；添加入口也占据整行，不使用方形网格。

- 全站文件行中的图片统一显示真实缩略图；此规则应用于资料列表、安排详情和个人页，PDF 保留文件图标。

- 文件行的真实图片缩略图沿用 file-icon 的 −6° 倾斜、白色边框与圆角。

## 通用版本

- 注册与登录沿用纸张和淡紫色风格。首次使用展示空行程状态，不填充虚构内容。
- 认证表单按照亲密关系安排间距：标签与输入框 6px，字段之间 16px，字段区域与提交区域 20px，错误提示与主按钮 8px，主次按钮之间 12px；辅助操作独立分组。消除默认 margin 与 gap 的叠加，保持紧凑但清楚的层级。
- 我的行程使用卡片列表，创建与设置使用抽屉。首页维持票券布局，不加入顶部品牌横幅。
- 行程安排编辑支持起终点、当地时区、来源、提醒与关联文件。手动添加时自动记录来源，不要求用户填写。
- 个人页面按“个人资料、我的行程、证件与文件、账号”分组：头像卡右侧提供编辑资料图标，行程切换与同行成员归入行程区，护照卡与证件文件合并为“证件与文件 · 仅本人可见”，修改密码与退出登录归入底部账号区。具体行程的设置、邀请和删除在行程管理详情内操作。
- 无行程首页使用空状态票券引导，底部导航始终可用；空状态提供“创建行程”（直接进入创建流程）和“逛逛旅行攻略市场”（直接打开市场）两个入口。个人页提供“我的行程”入口；管理页以返回操作、创建／加入入口和行程票券列表组织内容，不能包含个人名片或退出按钮。
- 当前行程选择仅位于“我的”页面，通过抽屉切换并停留在个人页。底部导航恢复原有单层图标布局，其他页面不显示额外行程控件。行程卡片显示创建／加入关系、日期与当前状态，点击进入管理详情，不自动切换行程。
- 行程管理详情提供返回列表、状态、设置行、邀请区和独立删除区。邀请提供生成、复制和确认撤销；非创建者不展示管理权限操作。完整行程和旅行资料在原有标题右侧提供圆形加号，保留原有内容起始位置与筛选方式，不增加独立操作行。
- 邀请口令采用 6 位大写字母，使用单行大字号与适当字距展示，加入入口为单行输入框，支持手动输入或粘贴。有效期内再次查看显示同一口令，不自动更换。
- 创建表单主要展示目的地和旅行日期，常用设置按需展开；加入表单只包含邀请说明、口令和提交反馈。注册不要求昵称，个人资料使用“昵称”标签。

## 列表空态

- 登录只展示邮箱、密码和提交区域；注册和找回密码在生产环境展示验证码。
- 空列表不渲染空白 surface 容器或分隔线。使用纸张卡片承载图标、标题和简短说明，内容区域至少 172px，保留原页面标题与筛选的位置。
- 首次使用引导添加数据；筛选无结果引导调整或清除筛选。准备清单为空时隐藏零进度条，安排未关联凭证时显示明确说明。

## 简化录入

- 创建行程先选择目的地与日期，名称可以自动生成；支持多个目的地，时间与币种以中文显示并按需调整。
- 添加安排使用五种类型按钮，核心字段根据活动、航班、住宿、自驾、交通改变。活动默认只需名称和日期，住宿使用入住/退房日期；具体时刻可稍后填写。
- 时间不确定的安排显示“时间待定”，不展示虚假倒计时；当地时钟重复时让用户选择第一次或第二次，不要求输入 UTC 偏移。
- 可选字段收在“更多信息”，分类按钮与字段组之间 18px，标签与输入之间 6–8px，可选区域内部 14px。单个日期字段使用完整宽度；320px 屏幕双列日期改为单列。
- 资料上传先明确“同行成员可见 / 仅自己可见”，支持批量、缩略图、逐份进度及只重试失败文件。安排内上传后自动勾选关联，取消安排仍保留已经上传的资料。
- 账本使用行程两种币种的中文切换按钮、分摊成员快捷选择与逐人金额；金额输入保持原有视觉层级。
- 昵称与头像是个人资料的主要内容，证件信息默认折叠且仅本人可见。头像保存后继续编辑，不丢失昵称。
- 关闭编辑时保护未保存内容；保存失败保留表单，字段错误就近展示。已完成的连续添加不要求重复放弃确认。

## 安排步骤与资料选择

- 添加和编辑安排均分为类型和名称、时间、地点、资料四步，上一步保留已填写信息。
- 关联资料使用三列正方形缩略图，点击整个方框选中或取消，边框与勾选状态同步；非图片展示文件图标。
- 资料上传首先展示分类与文件，分类可以选择或输入新名称；可见范围折叠为小字号高级设置，摘要始终展示当前范围。

- 安排表单按照步骤展示相关字段，输入文字保持 16px、主按钮至少 44px，关联资料始终展开，随弹窗内容区滚动。

- 所有弹窗复用 Sheet；高度随内容变化，手机端最大高度为 min(860px, 92dvh)，桌面端为 min(860px, 100dvh - 48px)。标题固定，中间内容独立滚动，操作按钮通过 SheetFooter 固定在底部；表单使用 SheetForm 保留原生提交和校验。类型选择和未保存提示按内容自然缩小，不设置固定高度或最小高度。关联资料缩略图复用 file-icon 的 -6° 倾斜、3px 白边、圆角和阴影，选中时附加淡紫色描边与勾选。
- 表单弹窗标题底部 6px、内容顶部 8px，避免间距相加造成空白；准备清单空态与添加按钮之间保留 18px。

## 安排四步录入

类型和名称、时间、地点、资料分别展示，底部提供上一步与下一步，最后一步保存；不显示 n/m 或顶部步骤说明。前后切换、上传完成和保存失败均保持草稿。

时间使用 Ant Design TimePicker（24 小时、精确到分钟、inputReadOnly）、Switch、纯选择 Select（showSearch=false）。时分与当地时间点击只展开选项，不唤起手机软键盘。时间与当地时间并排，下面提供时间段开关，开启后展示结束日期及当地时间；不选择时间时记录为时间待定。浮层挂载在弹窗内部、滚动区域外，关闭组件库动画以兼容减少动态效果。

地点支持关键词搜索与候选选择，自动保存地址和经纬度。资料展示可选贴纸缩略图，并在同一网格中提供方形上传入口。

## 旅行资料上传

分类使用 Ant Design Select，输入新名称后选择“新建分类”，与当前行程已有可见资料分类合并去重。下拉浮层位于弹窗内且不占内容高度。文件入口复用关联资料的三列方框与贴纸样式；上传时对应方框覆盖圆形真实进度，文件名和状态位于方框下方。保留批量选择、移除、暂停、失败重试与底部上传操作。

资料预览底部与打开、下载并列提供“修改名称”。编辑使用紧凑的 Sheet，预填当前名称，底部保存；失败保留输入，保存后立即更新标题和资料搜索。

安排、资料页、账本上传与预览统一使用“资料”称呼。安排详情的“关联资料”直接打开编辑器的资料步骤，可以返回前面的步骤，保留原安排内容。

## 行程分享与市场

- 市场入口放在“我的行程”，保留原有底部导航。市场卡片沿用淡紫色票券样式，展示目的地、天数与安排数量。
- 公开预览按日期分组展示安排，使用真实当地时间与地点。复制操作固定在可见区域，确认弹窗仅要求选择出发日期。
- 发布前展示与公开页面一致的内容预览，清楚说明公开范围，点击发布后展示二次确认，不使用勾选框；公开链接、更新与取消分享统一放在行程管理。
- 公开分享可设置“公开名称”，默认预填原行程名，选填且最多 100 字；覆盖名用于市场卡片、分享预览、搜索与他人复制的行程名，不修改原行程。
- 分享和复制使用通用 Sheet，内容自然高度、最大高度约束、正文可滚动，操作按钮保留在底部。

## 安排状态

- 完整行程卡片用紧凑标签显示“已结束”或“已取消”，保留原有名称、时间与资料入口。
- 详情提供结束、取消和恢复操作，说明只改变应用内安排，不取消实际预订。首页所有安排入口统一跳过手动结束、取消的安排；恢复后按照原计划时间判断显示。

- 公开预览与分享预览使用同一个 SnapshotView；真实路线复用 TicketRoute 展示左右起终点及当地时间，中央保留 TravelSticker，活动和住宿使用对应实物贴纸。管理入口统一 12px 间距，市场与创建/加入入口共用布局和 17px 标题字号。

- 旅行攻略市场显示作者圆形头像和昵称，头像缺失时使用昵称首字。分享默认展示独立口令，不要求复制链接；搜索支持口令、不区分大小写。发布介绍最多 500 字并显示计数，市场卡片使用灰色单行省略区域，详情完整显示换行内容；发布确认说明作者信息与介绍公开。

## 路线图与行程海报

- 路线图入口是完整行程页标题行「添加安排」左侧的地图图标按钮（`aria-label="查看路线图"`），不新增底部导航项或独立操作行；路线图以全屏 Sheet 打开，标题右侧提供「生成海报」文字入口。
- 地图使用 MapLibre + OpenFreeMap 底图，不显示底图署名。查看状态下每个地点是无序号圆点加地名：起点绿色、终点红色、中途紫色；地名用地图原生标注渲染（自动避让，密集处放大后逐级出现），点击圆点或地名打开对应安排；底图地名文字整体放大一档。路线按开始时间排序，航班段为虚线大圆弧，其余为实线。
- 每段路线在靠近终点的位置放一个方向箭头，方向由该段最后走向决定（不沿线重复排布）；航段用飞机贴纸、其余段用车辆贴纸，放在段的中间位置。贴纸大小 = 地图缩放档位 × 线路长度系数，曲线非线性（0.12–1.5 倍）：长段如航班保持大尺寸，短段迅速变小，避免密集的城市间移动挤满贴纸、盖住地名；贴纸始终绘制、不因碰撞被取舍，地名标注绘制在贴纸之上。
- 地图上方按日期提供紧凑胶囊筛选（「全程」+ 各日期）。线段属于它出发的那一天；选中某天时只保留当天的线段、箭头与贴纸，并显示这些线段两端的地点（当天没有线段时显示当天到访的地点），把地图平移缩放到当天路线范围（留一点 buffer）；切回「全程」恢复完整路线与全部地点。底部用白色胶囊明示缺坐标安排数量，可展开列表并点开详情；缺坐标不为 0 时才出现。
- 右下角提供播放按钮：点击后先清空静态路线与站点，从起点开始由载具（航段飞机、其余车辆）拖着线逐段画到终点，每段都是「先让镜头缩放到位、再开始拖线前进」，缩放与前进不同时发生，走完一段才落下该段的方向箭头，播放中不放线段贴纸、只有当前移动的载具是贴纸，结束后回到查看状态才显示全部贴纸；缩放到位阶段载具停在段起点不动，镜头中心与缩放用 ease-in-out 推到位（时长随缩放差变化）；就位后还要等这一屏底图瓦片加载完才开始拖线前进（最多等 5 秒，等待期间时间线冻结），避免录到空白瓦片。播放中始终用稍大的圆点 + 地名标出当前一段的起点与终点，段落切换一目了然。载具只显示贴纸本身（无圆形底衬、不再加 CSS 投影），大小随缩放与段长变化，但播放时画面里只有这一个贴纸，因此整体比线段贴纸放大一档（44–150px）。播放期间隐藏全部控件（标题栏与关闭、日期筛选、缺坐标提示、进度条与播放按钮），画面只留地图与路线动画，方便录屏；点屏幕任意位置临时显示控件，再次播放时重新隐藏。
- 播放镜头始终让当前载具居中，缩放按当前段起终点取景后再退一档，保证起点与终点都在画面内。每段时长随长度次线性增长（长段更快）且短段有下限，段内用 ease-in-out 变速，起步与到达前后放慢；缩放变化按每秒上限平滑过渡，避免「长途航班 → 短途自驾」时画面突然跳变。底部显示进度条，播放中锁定地图交互与日期筛选，可随时停止，结束后恢复完整路线并显示为「重新播放」。播放为纯前端动画，方便用户自行录屏。
- 底图加载失败或 WebGL 不可用时显示错误说明与按时间排列的站点文本列表，不出现空白画布；全部安排无坐标时使用 EmptyState 引导补充地点。
- 行程海报是 9:16（1080×1920）全屏地图加底部腰封：地图铺满整张海报（含路线、每段一个方向箭头、交通贴纸与起点终点），底部腰封使用纸张纹理、**无圆角**、紧凑排布，依次放行程名、日期与天数、左右起终点、目的地 / 安排数 / 地点数标签；腰封最后一行是产品品牌与当前站点域名（如 `旅行计划 · trip.example.com`），不展示地图数据来源。飞机贴纸压在右上角。
- 海报弹窗不显示标题栏与内边距，地图直接铺满弹窗宽度，关闭按钮以白色圆形悬浮在左上角，底部只保留保存 / 分享操作；文案尽量精简，不放说明性段落。
- 海报始终按固定尺寸渲染，地图截图完成前只在同一容器上覆盖一层加载占位（转圈 +「正在加载地图…」），不使用地点文字列表；加载完成只移除占位，弹窗高度不会跳动。海报按弹窗宽度与可用高度取较小值等比缩放，保证整张可见且弹窗内部不滚动。隐藏的地图实例必须脱离文档流（选择器要压过 MapLibre 的 `.maplibregl-map`），否则地图初始化期间会撑高弹窗。
- 地图取景为路线整体范围并预留腰封高度，保证路线不被腰封遮挡；截图取独立隐藏的 MapLibre 实例，生成或关闭后销毁实例。
- 海报只包含行程名、日期、天数、目的地、安排数量、地图与地名，不展示成员、账本、资料、电话、预订编号、私人备注；预览按屏幕宽度等比缩放，生成后优先系统分享，不支持时下载图片，失败就近提示可重试并保留预览。
