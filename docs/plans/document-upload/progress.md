# 旅行资料上传优化

- [x] 分类改为可搜索、可创建的组件库 Select，复用当前行程可见文件分类。
- [x] 文件采用贴纸方框网格，上传进度以方框内圆形进度显示，保留暂停、移除和失败重试。
- [x] 验证分类跨弹窗、页面刷新及安排上传复用，检查手机布局，执行测试并提交。

## 实现与验证

分类原本随文件保存在数据库，旧入口使用原生 datalist。现在统一使用 Ant Design Select，从当前可见资料合并已有分类，支持搜索与明确的新建操作；无需数据库迁移。旅行资料页和安排内嵌上传共用组件。

文件选择改为三列贴纸方框与加号入口，使用 Ant Design Progress 在当前文件方框中央展示真实上传百分比，保留批量选择、移除、暂停与失败重试。Escape 优先关闭展开的 combobox，不连带关闭 Sheet。

验证：类型检查、生产构建、20 项业务测试、Chromium 与 WebKit 各 10 项流程测试全部通过；覆盖新分类上传、重新打开与刷新后复用、安排内重复上传的分类选项、圆形进度显示、失败重试不重复上传。检查两种浏览器手机尺寸截图，源码审计与 git diff 检查通过。本地 http://localhost:8790 返回 200，已经更新构建；本次没有发布生产。

## 资料名称修改

- [x] 增加行程资料重命名接口，验证成员资格、私有权限、名称及并发修改。
- [x] 预览弹窗提供修改名称入口，同步资料列表、关联资料和搜索，下载保留正确扩展名。
- [x] 验证保存、失败重试、搜索与刷新持久性，执行测试并提交。

资料名称修改验证：类型检查与构建通过；21 项业务测试、Chromium / WebKit 各 11 项流程测试通过。覆盖名称校验、跨行程与私有资料拒绝访问、并发修改冲突、文件字节保持一致、下载扩展名、失败保留草稿、新名称搜索和刷新持久性。测试浏览器使用独立的本地限流标识，避免不同浏览器批量登录触发同一限流计数。源码审计通过，本地 8790 已更新；没有数据库迁移，没有发布生产。

## 资料称呼与安排入口

安排详情的“关联资料”直接进入 EventEditor 第四步，普通修改仍从第一步开始，允许上一步、下一步切换并保存。页面、无障碍标签和服务端错误统一使用“资料”，不修改用户文件名称及内部 receipts 标识。类型检查、构建、21 项业务测试、Chromium / WebKit 各 11 项流程测试通过，覆盖详情直达、步骤切换和关联保存。源码审计通过，本地预览已经更新，本次没有发布生产。

## 生产发布

2026-09-18 已发布分类 Select 与创建选项、方框上传和圆形进度、资料改名与搜索、资料称呼统一、安排资料步骤直达及“添加更多资料”入口。代码版本 a78ae45，Worker 版本 1535c400-02f6-49a9-8c51-af1b98b03103。部署确认无数据库迁移，私有 R2 检查通过。线上首页返回 200，9 个首页引用的 JavaScript / CSS 资源逐一与本地构建 SHA-256 一致，生产邮箱验证配置正常。本次发布不修改历史业务数据。
