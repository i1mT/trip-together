# 安排四步录入

- [x] 类型与名称、时间、地点、资料四步及草稿保持
- [x] Ant Design TimePicker、时间段 Switch、浮层时区 Select
- [x] 全球地点搜索、坐标保存与导航适配
- [x] 资料网格上传与自动关联
- [x] 回归测试、本地预览与规范更新
- [x] commit

## 实现

所有安排从类型与名称开始，底部前后切换，顶部不显示步骤编号。字段、关联资料及搜索结果保持草稿；保存失败与关闭保护保留原体验。新安排取消确认复选框，默认计划中，不凭空标记已确认。

统一时间组件支持分钟、单个时刻、时间段、每端时区与时间待定，保留夏令时重复/缺失时刻校验。保存 timeRange 标志以正确恢复开关状态；各类安排详情展示结束时间。Ant Design 浮层位于弹窗焦点范围内、滚动内容外，不撑高弹窗，并关闭动画以兼容系统减少动态效果。

地点通过已登录的 Worker 接口代理 Geoapify，保存结果名称、完整地址及 WGS84 坐标。旧文字地址兼容保留。导航采用 Apple Maps 坐标路线链接，旧地址使用文字搜索。上传位于资料网格，上传后同时保存到旅行资料并自动关联。

## 验证

类型检查与本地构建通过。19 项 API/业务测试、Chromium 10 项与 WebKit 10 项浏览器测试验证分钟面板选择、跨时区时间、步骤往返、浮层高度、搜索失败重试、资料上传关联、持久化与草稿恢复。

真实本地接口搜索“上海迪士尼”“东京晴空塔”“Eiffel Tower Paris”均返回名称、地址及坐标。覆盖质量因地区和名称不同而变化，不能保证每个地点都有结果。

Key 仅保存在忽略文件 infra/.dev.vars；公开源码与 web/out 构建产物检查均未包含该 Key。源码审计通过。本地预览使用 8790；本次没有更新生产 Secret、没有发布线上版本。

## 地点搜索反馈改进

- [x] 搜索按钮增加旋转图标、aria-busy 与禁用状态，并遵循减少动态效果设置。
- [x] 普通文字查询无结果时按城市名称再次查询，两次请求共用 8 秒超时；服务错误不当作空结果。
- [x] 实测原接口“杭州市”有结果、“杭州”为空；增加城市查询后两者均返回杭州市。此前“杭州市”偶尔没有结果的现象没有复现。
- [x] 类型检查、构建、20 项业务测试与两种浏览器各 10 项测试通过；覆盖查询回退、无额外请求、错误区分和加载反馈。

## 手机选择器输入修正

TimePicker 启用 inputReadOnly，时区 Select 关闭 showSearch，点击只展开组件库选项，不接受时分或时区文字输入。浏览器回归测试改为实际点击小时、分钟和时区，并检查只读属性与保存结果；保留单点、时间段及跨时区能力。

本次修正通过类型检查、构建、20 项业务测试及 Chromium / WebKit 各 10 项流程测试，源码审计通过。本地预览已经更新。

2026-09-18 已发布至 https://trip.awell.one，Worker 版本 ddafaec7-2702-40fd-9232-51ede83bb0f2。线上首页及新 JavaScript 资源返回 200，资源 SHA-256 与本地构建一致，生产邮箱验证配置正常；R2 私有访问检查通过。此次没有数据库迁移，不修改历史业务数据。
