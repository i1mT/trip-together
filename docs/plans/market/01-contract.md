# 公开数据契约

## 目标与交付
`shared/market.ts` 以字段白名单定义公开 trip/event/snapshot。`0007_public_itineraries.sql` 独立保存公开快照与复制幂等记录，原有私人行程不自动公开。

## 完成事项
- [x] 公开名称、日期、当地时区、目的地、事项类型和地点路线。
- [x] 排除资料、成员、账本、电话、预订编号、备注、准备清单。
- [x] 持久化公开版本，复制记录不随撤销公开内容删除。

## 验证
运行时解析 schema，公开返回体断言不含私人字段，复制副本没有资料与成员关系。审核发现 refined schema 的 pick 限制，已经改用 shape 重新建立公开 schema 并显式验证日期。
