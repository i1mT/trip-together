# 通用版本生产发布

- [x] 确认独立生产资源与待执行迁移：仅新增 destinations 字段，默认空数组
- [x] 保存 D1 Time Travel 恢复点（本地私密发布记录）
- [x] 生产地点搜索 Secret 配置
- [x] 发布及线上验证
- [x] 发布后数据检查、记录与 commit

## 发布结果

部署源码 commit：a0a6df0。Wrangler dry-run、数据库迁移、Worker 与静态资源发布均成功。生产 Secret 已设置，原有会话签名密钥未更换。R2 发布前后均确认私有，没有更改文件存储访问方式或写入业务文件。

此次唯一迁移为 0005_trip_destinations.sql：ALTER TABLE 增加 destinations 字段，默认空数组；没有清空、重建数据库或覆盖既有字段，没有操作旧私人版本资源。

线上页面、真实登录、空白账号页面、生产邮箱验证配置及“杭州”“杭州市”真实地点搜索均验证通过。验证使用独立临时账号，完成后已删除账号及其会话，没有创建业务行程或支出。

## 数据检查的范围

发布前 Time Travel bookmark 保存在忽略目录 .local/releases/current。发布前后尝试的 UNION 统计经 Wrangler 文件导入执行时被 D1 拒绝，未形成有效的发布前数量基线，不能声称完成数量或内容逐项对比。随后改为独立 SELECT 完成现有成员、行程、同行关系、安排、支出、准备事项与文件记录数量检查。部署迁移日志确认仅执行上述加字段 SQL。

具体生产资源、版本 ID、恢复点与测试日志均保存在本地忽略目录，不包含在公开源码中。
